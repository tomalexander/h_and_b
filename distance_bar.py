import pygame
from generic_bar import generic_bar

class distance_bar(generic_bar):
    """the player's coy fish"""
    def __init__(self):
        generic_bar.__init__(self) #call Sprite initializer
